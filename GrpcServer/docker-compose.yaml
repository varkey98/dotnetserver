networks:
  local-net:
    external: false
services:
  envoy:
    image: envoyproxy/envoy:v1.35-latest
    ports:
      - "8080:8080"
    volumes:
      - ./envoy.yaml:/etc/envoy/envoy.yaml
    networks:
      - local-net
  grpcserver:
    build:
      context: .
      dockerfile: Dockerfile  
    networks:
      - local-net