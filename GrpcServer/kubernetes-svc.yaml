apiVersion: v1
kind: Service
metadata:
  name: grpcserver
  labels:
    run: grpcserver
spec:
  ports:
  - port: 5001
    protocol: TCP
    appProtocol: grpc # This is required for gRPC
  selector:
    run: grpcserver